# Changelog — workstate-stack

## Unreleased

## [0.1.3] — 2026-06-07

### Changed

- Pin bumps: `workstate-bootstrap==0.8.5`, `workstate-system==0.2.3`
  (implementation note MCP registration single-source ownership),
  `workstate-protocol==0.2.3`, `mcp-workstate-handoff==0.12.5`,
  `mcp-workstate-orchestrator==0.6.2` (WORKSTATE-REF-09 grok harness parity).


## [0.1.2] — 2026-06-06

### Changed

- Bump pinned workstate-bootstrap to 0.8.4 (HARNESS_PLUGIN_DELIVERY registry,
  managed gitignore reconciliation, adopt --check drift detection).


## [0.1.1] — 2026-06-06

### Changed

- First published cut of the meta-package. Pins:
  `workstate-protocol==0.2.2`, `mcp-workstate-handoff==0.12.4`,
  `mcp-workstate-orchestrator==0.6.1`, `workstate-bootstrap==0.8.3`,
  `workstate-system==0.2.2`.


## 0.1.0 (unreleased)

- New meta-package: single version anchor with exact `==` pins on the
  published Workstate runtime stack (`workstate-protocol`,
  `mcp-workstate-handoff`, `mcp-workstate-orchestrator`,
  `workstate-bootstrap`, `workstate-system`). Pins are generated by
  `scripts/stack_pins.py` (`make stack-pins-sync`) and gated by
  `make stack-pins-check` (implementation note implementation note).
